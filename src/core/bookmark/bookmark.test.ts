Deno.test("bookmark-id", (t) => {
  t.step("should create a bookmark id", (t) => {
    t.step("should accept valid bookmark id format", () => {});
    t.step("should reject invalid bookmark id format", () => {});
    t.step("should be equal when ids are the same", () => {});
  });
});
